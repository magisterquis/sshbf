Interactive commands:
        List active attacks
        Quit
Fix out of guesses message when password found
# Make sorting work for l command
# Actually kill hostmasters when they're done
# Add fails file.
# Have hostmaster die if an address fails resolution
Adaptive m per hostmaster
# Get/set global number of tasks (M)
Get running with m as well
Better l output
g shows number of hosts being attacked
Track number of retries
Keep track of how long hostmasters take to execute

